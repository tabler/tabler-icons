plugins:
  - jekyll-last-modified-at


host: 0.0.0.0

exclude:
  - .idea
  - package.json

defaults:
  - scope:
      type: "icons"
    values:
      layout: icon
      permalink: 'icons/tabler-:slug'

collections:
  icons:
    output: true
