<svg xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="butt"
  stroke-linejoin="miter">

  <!-- Second place (filled + stroke) -->
  <rect x="5" y="12" width="4" height="8"
        fill="currentColor" />

  <!-- First place (hollow, tallest) -->
  <rect x="10" y="8" width="4" height="12"
        fill="none" />

  <!-- Third place (filled + stroke) -->
  <rect x="15" y="14" width="4" height="6"
        fill="currentColor" />
</svg>
